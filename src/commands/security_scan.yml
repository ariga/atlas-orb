# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Scan databases for security issues with Atlas

parameters:
  fail_on:
    type: enum
    default: ''
    description: |
      The lowest severity that fails the action. By default, reported issues do not
      fail it, and only a database that could not be scanned does.

    enum:
      - ''
      - NORMAL
      - ELEVATED
      - HIGH
      - CRITICAL
  ignore:
    type: string
    default: ''
    description: |
      CVE identifier(s) not to report. For example: `CVE-2017-18359`.
  min_severity:
    type: enum
    default: ''
    description: |
      The lowest severity to report. For example, `HIGH` reports only the `HIGH`
      and `CRITICAL` issues.

    enum:
      - ''
      - NORMAL
      - ELEVATED
      - HIGH
      - CRITICAL
  urls:
    type: string
    default: ''
    description: |
      URL(s) of the target databases, one per line.
      For example: `postgres://localhost:5432/app?sslmode=disable`.
      If not set, Atlas uses the [`url`](https://atlasgo.io/hcl/config#env.url) from the config file.
      Read more about [Atlas URLs](https://atlasgo.io/concepts/url).
  working_directory:
    type: string
    default: '.'
    description: |
      Atlas working directory. Default is project root
  config:
    type: string
    default: ''
    description: |
      The URL of the Atlas configuration file. By default, Atlas will look for a file
      named `atlas.hcl` in the current directory. For example, `file://config/atlas.hcl`.
      Learn more about [Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    type: string
    default: ''
    description: |
      The environment to use from the Atlas configuration file. For example, `dev`.
  vars:
    type: string
    default: ''
    description: |
      A JSON object containing variables to be used in the Atlas configuration file.
      For example, `{"var1": "value1", "var2": "value2"}`.

steps:
  - run:
      name: Security Scan
      command: |
        atlasaction --action security/scan
      environment:
        ATLAS_INPUT_FAIL_ON: <<parameters.fail_on>>
        ATLAS_INPUT_IGNORE: <<parameters.ignore>>
        ATLAS_INPUT_MIN_SEVERITY: <<parameters.min_severity>>
        ATLAS_INPUT_URLS: <<parameters.urls>>
        ATLAS_INPUT_WORKING_DIRECTORY: <<parameters.working_directory>>
        ATLAS_INPUT_CONFIG: <<parameters.config>>
        ATLAS_INPUT_ENV: <<parameters.env>>
        ATLAS_INPUT_VARS: <<parameters.vars>>
