# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Automatically generate a hash of the schema migrations directory, and commit it to the migration directory.

parameters:
  base_branch:
    type: string
    default: ''
    description: |
      The base branch to rebase the migration directory onto. Default to the default branch of the repository.
  dir:
    type: string
    default: ''
    description: |
      The URL of the migration directory to hash. By default: `file://migrations`.
  remote:
    type: string
    default: ''
    description: |
      The remote to fetch from. Defaults to `origin`.
  working_directory:
    type: string
    default: '.'
    description: |
      Atlas working directory. Default is project root
  config:
    type: string
    default: ''
    description: |
      The URL of the Atlas configuration file. By default, Atlas will look for a file
named `atlas.hcl` in the current directory. For example, `file://config/atlas.hcl`.
Learn more about [Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    type: string
    default: ''
    description: |
      The environment to use from the Atlas configuration file. For example, `dev`.

steps:
  - run:
      name: Migrate Hash
      command: |
        atlasaction --action migrate/hash
      environment:
        ATLAS_INPUT_BASE_BRANCH: <<parameters.base_branch>>
        ATLAS_INPUT_DIR: <<parameters.dir>>
        ATLAS_INPUT_REMOTE: <<parameters.remote>>
        ATLAS_INPUT_WORKING_DIRECTORY: <<parameters.working_directory>>
        ATLAS_INPUT_CONFIG: <<parameters.config>>
        ATLAS_INPUT_ENV: <<parameters.env>>
