# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Reverts deployed migration files on a target database

parameters:
  amount:
    type: integer
    default: ''
    description: |
      The amount of applied migrations to revert. Mutually exclusive with `to-tag` and `to-version`.
  dir:
    type: string
    default: ''
    description: |
      The URL of the migration directory to apply. For example: `atlas://dir-name` for cloud
      based directories or `file://migrations` for local ones.
  revisions_schema:
    type: string
    default: ''
    description: |
      The name of the schema containing the revisions table.
  to_tag:
    type: string
    default: ''
    description: |
      The tag to revert to. Mutually exclusive with `amount` and `to-version`.
  to_version:
    type: string
    default: ''
    description: |
      The version to revert to. Mutually exclusive with `amount` and `to-tag`.
  url:
    type: string
    default: ''
    description: |
      The URL of the target database. For example: `mysql://root:pass@localhost:3306/dev`.
  wait_interval:
    type: string
    default: '1s'
    description: |
      Time in seconds between different migrate down attempts.
  wait_timeout:
    type: string
    default: ''
    description: |
      Time after which no other retry attempt is made and the action exits.
  working_directory:
    type: string
    default: '.'
    description: |
      Atlas working directory. Default is project root
  config:
    type: string
    default: ''
    description: |
      The URL of the Atlas configuration file. By default, Atlas will look for a file
      named `atlas.hcl` in the current directory. For example, `file://config/atlas.hcl`.
      Learn more about [Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    type: string
    default: ''
    description: |
      The environment to use from the Atlas configuration file. For example, `dev`.
  vars:
    type: string
    default: ''
    description: |
      A JSON object containing variables to be used in the Atlas configuration file.
      For example, `{"var1": "value1", "var2": "value2"}`.
  dev_url:
    type: string
    default: ''
    description: |
      The URL of the dev-database to use for analysis. For example: `mysql://root:pass@localhost:3306/dev`.
      Read more about [dev-databases](https://atlasgo.io/concepts/dev-database).

steps:
  - run:
      name: Migrate Down
      command: |
        atlasaction --action migrate/down
      environment:
        ATLAS_INPUT_AMOUNT: <<parameters.amount>>
        ATLAS_INPUT_DIR: <<parameters.dir>>
        ATLAS_INPUT_REVISIONS_SCHEMA: <<parameters.revisions_schema>>
        ATLAS_INPUT_TO_TAG: <<parameters.to_tag>>
        ATLAS_INPUT_TO_VERSION: <<parameters.to_version>>
        ATLAS_INPUT_URL: <<parameters.url>>
        ATLAS_INPUT_WAIT_INTERVAL: <<parameters.wait_interval>>
        ATLAS_INPUT_WAIT_TIMEOUT: <<parameters.wait_timeout>>
        ATLAS_INPUT_WORKING_DIRECTORY: <<parameters.working_directory>>
        ATLAS_INPUT_CONFIG: <<parameters.config>>
        ATLAS_INPUT_ENV: <<parameters.env>>
        ATLAS_INPUT_VARS: <<parameters.vars>>
        ATLAS_INPUT_DEV_URL: <<parameters.dev_url>>
