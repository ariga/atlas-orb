# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Automatically resolves `atlas.sum` conflicts and rebases the migration directory onto the target branch.

parameters:
  base_branch:
    type: string
    default: ''
    description: |
      The base branch to rebase the migration directory onto. Default to the default branch of the repository.
  base_sha:
    type: string
    default: ''
    description: |
      Optional. When set, use this git commit SHA as the base instead of base-branch. Requires the git server to support fetch-by-SHA (e.g. GitHub, GitLab).
  dir:
    type: string
    default: ''
    description: |
      The URL of the migration directory to rebase on. By default: `file://migrations`.
  force_rebase:
    type: boolean
    default: false
    description: |
      When true, skip the merge step and rebase whenever there are dev-only migrations (e.g. out-of-order workflow after merging base and running apply with exec-order non-linear).
  remote:
    type: string
    default: ''
    description: |
      The remote to fetch from. Defaults to `origin`.
  working_directory:
    type: string
    default: '.'
    description: |
      Atlas working directory. Default is project root

steps:
  - run:
      name: Migrate Auto Rebase
      command: |
        atlasaction --action migrate/autorebase
      environment:
        ATLAS_INPUT_BASE_BRANCH: <<parameters.base_branch>>
        ATLAS_INPUT_BASE_SHA: <<parameters.base_sha>>
        ATLAS_INPUT_DIR: <<parameters.dir>>
        ATLAS_INPUT_FORCE_REBASE: <<parameters.force_rebase>>
        ATLAS_INPUT_REMOTE: <<parameters.remote>>
        ATLAS_INPUT_WORKING_DIRECTORY: <<parameters.working_directory>>
