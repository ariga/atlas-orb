# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Sync the database schema to Atlas Cloud.

parameters:
  collect_stats:
    type: boolean
    default: true
    description: |
      Whether to collect and send anonymized usage statistics to Atlas.
  exclude:
    type: string
    default: ''
    description: |
      List of glob patterns used to select which resources to filter in inspection
see: https://atlasgo.io/declarative/inspect#exclude-schemas
  include:
    type: string
    default: ''
    description: |
      List of glob patterns used to select which resources to keep in inspection
see: https://atlasgo.io/declarative/inspect#include-schemas
  schemas:
    type: string
    default: ''
    description: |
      List of database schemas to include (by default includes all schemas). see: https://atlasgo.io/declarative/inspect#inspect-multiple-schemas
  slug:
    type: string
    default: ''
    description: |
      Optional unique identifier for the database server.
  url:
    type: string
    default: ''
    description: |
      URL of the database to sync (mutually exclusive with `config` and `env`).
  config:
    type: string
    default: ''
    description: |
      The URL of the Atlas configuration file (mutually exclusive with `url`).
For example, `file://config/atlas.hcl`, learn more about
[Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    type: string
    default: ''
    description: |
      The environment to use from the Atlas configuration file.
For example, `dev` (mutually exclusive with `url`).
  cloud_token_env:
    type: env_var_name
    default: ATLAS_TOKEN
    description: |
      Environment variable containing the Atlas Cloud token.
      If provided, the command will authenticate to Atlas Cloud.
      (e.g. `ATLAS_TOKEN`)

steps:
  - run:
      name: Atlas Schema Monitoring
      command: |
        ATLAS_INPUT_CLOUD_TOKEN=${<<parameters.cloud_token_env>>}
        atlasaction --action monitor/schema
      environment:
        ATLAS_INPUT_COLLECT_STATS: <<parameters.collect_stats>>
        ATLAS_INPUT_EXCLUDE: <<parameters.exclude>>
        ATLAS_INPUT_INCLUDE: <<parameters.include>>
        ATLAS_INPUT_SCHEMAS: <<parameters.schemas>>
        ATLAS_INPUT_SLUG: <<parameters.slug>>
        ATLAS_INPUT_URL: <<parameters.url>>
        ATLAS_INPUT_CONFIG: <<parameters.config>>
        ATLAS_INPUT_ENV: <<parameters.env>>
