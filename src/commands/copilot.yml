# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Talk to Atlas Copilot.

parameters:
  gen_test:
    type: boolean
    default: false
    description: |
      Generate atlas tests for the schema changes.

steps:
  - run:
      name: Atlas Copilot
      command: |
        atlasaction --action copilot
      environment:
        ATLAS_INPUT_GEN_TEST: <<parameters.gen_test>>
