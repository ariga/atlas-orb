# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Create a new repository in Atlas Cloud.

parameters:
  description:
    type: string
    default: ''
    description: |
      The description of the repository to create in Atlas Cloud.
  driver:
    type: string
    default: ''
    description: |
      The database driver to use for the repository.
  name:
    type: string
    default: ''
    description: |
      The name of the repository to create in Atlas Cloud.
  type:
    type: enum
    default: ''
    description: |
      The type of the repository, either "migration" or "schema".

    enum:
      - ''
      - migration
      - schema

steps:
  - run:
      name: Create Repository
      command: |
        atlasaction --action create-repo
      environment:
        ATLAS_INPUT_DESCRIPTION: <<parameters.description>>
        ATLAS_INPUT_DRIVER: <<parameters.driver>>
        ATLAS_INPUT_NAME: <<parameters.name>>
        ATLAS_INPUT_TYPE: <<parameters.type>>
