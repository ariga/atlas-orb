# Generated by go run ./cmd/gen orb-template; DO NOT EDIT.
description: >
  Applies a migration directory on a target database

parameters:
  allow_dirty:
    type: boolean
    default: false
    description: |
      Allow working on a non-clean database.
  amount:
    type: integer
    default: ''
    description: |
      The maximum number of migration files to apply. Default is all.
  dir:
    type: string
    default: ''
    description: |
      The URL of the migration directory to apply. For example: `atlas://dir-name` for cloud
based directories or `file://migrations` for local ones.
  dry_run:
    type: boolean
    default: false
    description: |
      Print SQL without executing it. Either "true" or "false".
  exec_order:
    type: enum
    default: ''
    description: |
      How Atlas computes and executes pending migration files to the database.
Either "linear", "linear-skip", or "non-linear".

    enum:
      - linear
      - linear-skip
      - non-linear
  revisions_schema:
    type: string
    default: ''
    description: |
      The name of the schema containing the revisions table.
  to_version:
    type: string
    default: ''
    description: |
      The target version to apply migrations to. Mutually exclusive with `amount`.
  tx_mode:
    type: enum
    default: ''
    description: |
      Transaction mode to use. Either "file", "all", or "none".

    enum:
      - file
      - all
      - none
  url:
    type: string
    default: ''
    description: |
      The URL of the target database. For example: `mysql://root:pass@localhost:3306/dev`.
  working_directory:
    type: string
    default: '.'
    description: |
      Atlas working directory. Default is project root
  config:
    type: string
    default: ''
    description: |
      The URL of the Atlas configuration file. By default, Atlas will look for a file
named `atlas.hcl` in the current directory. For example, `file://config/atlas.hcl`.
Learn more about [Atlas configuration files](https://atlasgo.io/atlas-schema/projects).
  env:
    type: string
    default: ''
    description: |
      The environment to use from the Atlas configuration file. For example, `dev`.
  vars:
    type: string
    default: ''
    description: |
      A JSON object containing variables to be used in the Atlas configuration file.
For example, `{"var1": "value1", "var2": "value2"}`.

steps:
  - run:
      name: Migrate Apply
      command: |
        atlasaction --action migrate/apply
      environment:
        ATLAS_INPUT_ALLOW_DIRTY: <<parameters.allow_dirty>>
        ATLAS_INPUT_AMOUNT: <<parameters.amount>>
        ATLAS_INPUT_DIR: <<parameters.dir>>
        ATLAS_INPUT_DRY_RUN: <<parameters.dry_run>>
        ATLAS_INPUT_EXEC_ORDER: <<parameters.exec_order>>
        ATLAS_INPUT_REVISIONS_SCHEMA: <<parameters.revisions_schema>>
        ATLAS_INPUT_TO_VERSION: <<parameters.to_version>>
        ATLAS_INPUT_TX_MODE: <<parameters.tx_mode>>
        ATLAS_INPUT_URL: <<parameters.url>>
        ATLAS_INPUT_WORKING_DIRECTORY: <<parameters.working_directory>>
        ATLAS_INPUT_CONFIG: <<parameters.config>>
        ATLAS_INPUT_ENV: <<parameters.env>>
        ATLAS_INPUT_VARS: <<parameters.vars>>
